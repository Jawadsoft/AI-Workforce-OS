export function AgentPermissions({ agentId }: { agentId: string }) {
  return <div className="rounded-lg border border-border p-6 text-muted-foreground text-sm">Agent permissions ({agentId}) - coming soon</div>
}
