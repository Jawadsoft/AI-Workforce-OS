export function AgentOverview({ agentId }: { agentId: string }) {
  return <div className="rounded-lg border border-border p-6 text-muted-foreground text-sm">Agent overview ({agentId}) - coming soon</div>
}
