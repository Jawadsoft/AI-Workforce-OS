export function AgentTools({ agentId }: { agentId: string }) {
  return <div className="rounded-lg border border-border p-6 text-muted-foreground text-sm">Agent tools ({agentId}) - coming soon</div>
}
